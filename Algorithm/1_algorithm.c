#if 0
#include <stdio.h>

int main(void)
{
	int a = 1, b = 3, c = 2;
	if ((b >= a && c <= a) || (b <= a && c >= a))
	{
		printf(a);
	}
	else if ((a > b && c < b) || (a<b && c>b))
	{
		printf(b);
	}
	printf(c);
}
#endif

#if 1
#include <stdio.h>

int main(void)
{
	printf("Hello World!\n");
	printf("Let's practice Visual Studio today!\n");
}
#endif


